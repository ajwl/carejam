
import React, { useRef, useEffect } from 'react';
import { gsap } from "gsap";
import '../App.css';

function Image({svg }) {
  return (
        <div className="svg-image-holder">
 
        </div>
  );
}

export default Image;
